# MCP server auth token — set a long random string in production
MCP_AUTH_TOKEN=change-me-in-production

# Port for the MCP HTTP server (default: 3001)
PORT=3001

# Path to the content directory (default: ../site/src/content relative to mcp/)
# In Docker, set this to /content and mount the volume there
CONTENT_DIR=
